#include <iostream>
#include <string>
#include <math.h>
#include<SDL.h>
using namespace std;
// #pragma once

class Window{
    public:
        //The window renderer
    static SDL_Renderer* gRenderer;
    //global reference to png image sheets
    static SDL_Texture* assets;
};
int main(void)
{
    return;
}